#ifndef SPHERE_H
#define SPHERE_H
#include "cudaTools.h"
#include "MathTools.h"
#include "cudaType_CPU.h"

class Sphere
{
public:
__host__
Sphere(float3 centre, float rayon, float hue)
{
// Inputs
this->centre = centre;
this->r = rayon;
this->hue = hue;
// Tools
this->rCarre = rayon * rayon;
}
